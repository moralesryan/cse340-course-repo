<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>
        <p>Explore our service categories:</p>
        <ul class="chip-list">
            <% categories.forEach(function(category) { %>
                <li>
                    <a class="chip" href="/category/<%= category.category_id %>">
                        <%= category.category_name %>
                    </a>
                </li>
                <% }) %>
        </ul>
    </main>

    <%- include('partials/footer') %>